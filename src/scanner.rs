//! The internal state of the memory scanner.
//!
//! Keeps track of all allocations and history and stuff

#[derive(Debug)]
pub struct Scanner { }

impl Scanner {
    pub fn new() -> Self {
        Self { }
    }
}
